# Nexus AI 智能对话助手

一个基于 **FastAPI** 和 **Ollama** 构建的现代化聊天机器人应用，拥有科技感前端界面和丰富的对话控制功能。支持流式响应、动态模型切换、生成参数调节、系统指令设定、自定义开场白等高级特性。

---

## ✨ 核心特性

### 🤖 强大的对话能力
- **流式输出**：AI 回答以逐字流式方式实时展示，体验流畅
- **多模型支持**：无缝切换任意 Ollama 模型（如 qwen2.5、llama3、mistral 等）
- **上下文记忆**：智能保留多轮对话历史，可配置保留轮数
- **对话中断**：支持手动停止生成，避免等待过长

### ⚙️ 丰富的参数控制
- **温度 (Temperature)**：调节随机性，让回答更严谨或更具创意（0.0 ~ 1.5）
- **Top-P 采样**：控制词汇多样性，影响输出风格
- **Max Tokens**：限制回答长度，适应不同场景
- **历史轮数**：精确控制携带的上下文数量，平衡记忆与资源

### 🎨 个性化定制
- **系统指令可视化**：在聊天区顶部实时显示当前 AI 的行为规则
- **自定义开场白**：自由设定 AI 的首次问候语，营造专属交互氛围
- **独立设置窗口**：所有高级选项集成于模态框，不占用聊天区域，界面简洁

### 🚀 现代技术体验
- **科技感 UI**：毛玻璃效果、渐变色彩、动态动画，视觉与交互俱佳
- **响应式设计**：自适应电脑、平板、手机屏幕
- **零依赖前端**：纯原生 HTML/CSS/JS，无需额外构建工具

---

## 🛠 技术栈

| 层级    | 技术                      | 说明                                   |
| ------- | ------------------------- | -------------------------------------- |
| 后端    | FastAPI + Uvicorn         | 异步 Web 框架，高性能处理流式请求      |
| AI 推理 | Ollama (OpenAI API 兼容)  | 本地部署大模型，无需 GPU 也可运行      |
| 前端    | HTML5 / CSS3 / JavaScript | 原生实现流式读取、AbortController 中断 |
| 通信    | Fetch API + Stream        | 流式接收数据，实时更新界面             |

---

## 📦 安装与运行

### 1. 环境要求
- Python 3.8 或更高版本
- [Ollama](https://ollama.com/) 已安装并运行（支持 Windows/macOS/Linux）
- pip 包管理工具

### 2. 安装 Python 依赖
```bash
pip install fastapi uvicorn openai
```

### 3. 下载模型（示例）

```bash
ollama pull qwen3.5:0.8b
```
*你可以替换为其他模型，如 `llama3`、`mistral`、`qwen2.5:7b` 等。*

### 4. 启动后端服务
将提供的 `main.py` 保存到本地目录，在终端执行：
```bash
python main.py
```
后端将在 `http://127.0.0.1:6066` 启动，终端输出类似：
```
INFO:     Uvicorn running on http://127.0.0.1:6066
```

### 5. 部署前端页面
在与 `main.py` 同级目录下创建 `static` 文件夹，将 `chat.html` 放入其中。  
访问：`http://127.0.0.1:6066/static/chat.html` 即可开始使用。

> **提示**：如果 Ollama 运行在非默认地址（如远程服务器），请修改 `main.py` 中的 `base_url`。

---

## 📖 详细使用指南

### 界面布局
- **顶部**：应用标题、当前模型状态、设置按钮（齿轮图标）
- **中间**：聊天消息区域，包括系统指令气泡和对话气泡
- **底部**：输入框、发送按钮、停止按钮

### 基础对话
1. 在输入框中输入消息，按 `Enter` 或点击“发送”按钮。
2. AI 的回答会逐字显示在聊天区。
3. 若想中断，点击“停止”按钮。
4. 新消息自动滚动到最底部。

### 参数调节（点击齿轮图标）
#### 模型中枢
- **模型名称**：输入已在 Ollama 中拉取的模型标签（如 `qwen3.5:0.8b`）。修改后下次请求立即生效。

#### 生成参数
| 参数         | 作用                                           | 推荐范围               |
| ------------ | ---------------------------------------------- | ---------------------- |
| 温度         | 值越高输出越随机、有创意；越低越保守、确定     | 0.2 ~ 1.2              |
| Top-P        | 限制采样词汇集，影响多样性                     | 0.5 ~ 0.9              |
| Max Tokens   | 限制回答的最大 token 数，留空则无限制          | 根据模型上下文长度设置 |
| 历史对话轮数 | 保留最近 N 轮对话（每轮包含 user + assistant） | 1 ~ 10                 |

#### 系统指令
- 设定 AI 的角色、语气或行为准则。修改后实时更新顶部气泡，并在后续请求中生效。

#### 开场白设置
- 自定义 AI 的首次问候语。点击“应用开场白并重置对话”会清空历史并显示新开场白。

#### 对话管理
- **仅清空对话记录**：清除所有用户与 AI 的对话，但保留开场白和系统指令。
- **应用开场白并重置对话**：完全重置，并以新开场白开始对话。

### 历史记忆机制
- 前端维护 `chatHistory` 数组，每次发送请求时根据“历史对话轮数”截取最近若干轮发送给后端。
- 后端将历史记录与系统指令、当前用户消息拼接后发送给模型，实现真正的多轮对话。

---

## 📁 文件结构

```
.
├── main.py                 # FastAPI 后端服务
└── static/
    └── chat.html           # 前端科技感聊天页面
```

### 后端代码说明 (`main.py`)
- 使用 `AsyncOpenAI` 客户端与 Ollama 的 `/v1` 接口通信
- 提供 `/chat` 路由，接收 POST 请求，支持：
  - `query`：用户输入
  - `sys_prompt`：系统提示词
  - `history`：历史对话列表
  - `history_len`：保留轮数
  - `temperature`、`top_p`、`max_tokens`：生成参数
  - `model`：模型名称（前端可传）
- 流式返回模型输出

### 前端代码说明 (`chat.html`)
- 采用 Flex 布局，深色主题，毛玻璃效果
- 模态框集中管理设置项
- 使用 `AbortController` 实现请求中断
- 通过 `ReadableStream` 解析流式数据，逐字更新界面

---

## 🛠 自定义与扩展

### 修改默认模型
- 在后端 `main.py` 中修改 `model` 参数，或通过前端设置窗口动态指定。

### 增加新的生成参数
1. 在后端 `/chat` 路由的 `Body` 中添加新参数（如 `frequency_penalty`）。
2. 在前端请求体中加入对应字段，并在模态框中添加控件。
3. 将参数传递给 `aclient.chat.completions.create()`。

### 更换前端主题
- 所有样式集中在 `<style>` 标签内，可轻松修改配色、圆角、字体等。

### 支持更多模型提供商
- 将 `base_url` 指向 OpenAI API 或兼容接口，即可使用云端模型。

---

## ❓ 常见问题与解决

### 1. 后端启动失败，提示 `ImportError`
- 确保已安装所有依赖：`pip install fastapi uvicorn openai`

### 2. 访问前端时提示“无法连接后端服务”
- 检查后端是否正常运行（终端无报错）
- 确认 Ollama 服务已启动（`ollama serve` 或在系统托盘中运行）
- 尝试在浏览器直接访问 `http://127.0.0.1:6066/chat` 是否返回 405（表示后端已运行）

### 3. 发送消息后没有响应或报错 500
- 查看后端终端输出，检查模型名称是否与已下载模型一致（`ollama list` 查看）
- 尝试降低 `max_tokens` 或减少历史轮数，避免超出上下文限制

### 4. 前端修改模型名后不生效
- 确保后端代码中 `model` 参数已从请求体读取（示例代码中已实现）
- 若后端未动态接收，请修改 `main.py` 添加 `model` 字段

### 5. 流式响应卡住或中断
- 可能是网络或模型生成过慢，尝试降低 `max_tokens` 或选择更小模型
- 点击“停止”按钮强制中断，再重试

### 6. 如何清空全部对话并重置开场白？
- 在设置窗口中修改开场白，点击“应用开场白并重置对话”即可。

---

## 🔗 相关资源

- [FastAPI 官方文档](https://fastapi.tiangolo.com/)
- [Ollama 官方文档](https://github.com/ollama/ollama)
- [OpenAI Python 库](https://github.com/openai/openai-python)

---

